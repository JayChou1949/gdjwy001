<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upd.hwcloud.dao.webManage.CaseMapper">

     <!--通用查询映射结果-->
    <resultMap id="BaseResultMap" type="com.upd.hwcloud.bean.entity.webManage.Case">
        <result column="ID" property="id" />
        <result column="NAME" property="name" />
        <result column="URL" property="url" />
        <result column="CONTENT" property="content" />
        <result column="IMAGE_ID" property="imageId" />
        <result column="SORT_NUM" property="sortNum" />
        <result column="CREATE_TIME" property="createTime" />
        <result column="MODIFIED_TIME" property="modifiedTime" />
        <result column="STATUS" property="status" />
        <result column="CREATOR" property="creator" />
        <association property="user" column="CREATOR" select="com.upd.hwcloud.dao.UserMapper.findUserByIdcard" />
    </resultMap>

    <select id="selectList" resultType="com.upd.hwcloud.bean.entity.webManage.Case">
        SELECT c.ID,c.URL,f.URL AS imageUrl FROM TB_CASE c
        LEFT JOIN TB_FILES f ON c.IMAGE_ID = f.ID
        WHERE c.STATUS = 2
        ORDER BY c.SORT_NUM ASC, c.MODIFIED_TIME DESC
    </select>

    <select id="getPage" resultMap="BaseResultMap">
        SELECT c.*,f.URL AS imageUrl FROM TB_CASE c
        LEFT JOIN TB_FILES f ON c.IMAGE_ID = f.ID
        WHERE
        <choose>
            <when test="status != null and status == 0">
                c.STATUS IN (0, 3)
            </when>
            <otherwise>
                c.STATUS = #{status}
            </otherwise>
        </choose>
        <if test="name != null and name != ''">
            AND c.NAME LIKE CONCAT(CONCAT('%',#{name}),'%')
        </if>
        <if test="user.type != 100">
            AND CREATOR = #{user.idcard}
        </if>
        ORDER BY SORT_NUM ASC, MODIFIED_TIME DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT c.*,f.URL AS imageUrl FROM TB_CASE c
        LEFT JOIN TB_FILES f ON c.IMAGE_ID = f.ID
        WHERE c.ID = #{id}
    </select>
</mapper>
