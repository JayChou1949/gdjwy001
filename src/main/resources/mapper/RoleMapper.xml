<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upd.hwcloud.dao.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.upd.hwcloud.bean.entity.Role">
        <id column="ID" property="id" />
        <result column="NUM" property="num" />
        <result column="PID" property="pid" />
        <result column="NAME" property="name" />
        <result column="CREATE_TIME" property="createTime" />
        <result column="MODIFIED_TIME" property="modifiedTime" />
        <result column="DESCRIPTION" property="description" />
    </resultMap>


    <select id="getPage" resultType="com.upd.hwcloud.bean.entity.Role">
        SELECT * FROM SYS_ROLE ORDER BY CREATE_TIME
    </select>

    <!-- 根据用户名查询相应角色 -->
    <select id="findRolesByUserId"
            resultMap="BaseResultMap">
        SELECT r.* FROM s_user_role ur,sys_role  r
        WHERE ur.user_id = #{uid}
        AND ur.role_id = r.id
    </select>
</mapper>
