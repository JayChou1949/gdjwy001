<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upd.hwcloud.dao.SubPortalViewMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.upd.hwcloud.bean.entity.SubPortalView">
        <id column="ID" property="id" />
        <result column="TYPE" property="type" />
        <result column="NAME" property="name" />
        <result column="VIEW_COUNT" property="viewCount" />
    </resultMap>
    
    <update id="incrementCount">
        UPDATE  TB_SUB_PORTAL_VIEW SET VIEW_COUNT = VIEW_COUNT + 1 WHERE  NAME = #{name} and TYPE = #{type}
    </update>

</mapper>
